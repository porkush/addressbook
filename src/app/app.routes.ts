import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const appRoutes: Routes = [
  { path: '', component: AppComponent },
  { path: './app.component.home.html', component: AppComponent },
  // Add other routes here
];
